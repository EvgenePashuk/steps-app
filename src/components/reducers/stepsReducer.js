const initialState = {
    cardNumber: '',
    country: '',
    postalCode: '',
    zip: ''
};

const stepsReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default stepsReducer;